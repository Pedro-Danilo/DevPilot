{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://devpilot.local/schemas/SCHEMA-DEVPL-PLANNING-EPIC-V1",
  "title": "Planning Epic",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_id",
    "schema_version",
    "id",
    "version",
    "title",
    "owner_role",
    "lifecycle",
    "trace_links",
    "milestone_id"
  ],
  "properties": {
    "schema_id": {
      "const": "SCHEMA-DEVPL-PLANNING-EPIC-V1"
    },
    "schema_version": {
      "const": "1.0.0"
    },
    "id": {
      "type": "string",
      "pattern": "^epic-[a-z0-9-]{2,59}$"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "owner_role": {
      "enum": [
        "owner",
        "product-owner",
        "architect",
        "developer",
        "qa-reviewer"
      ]
    },
    "lifecycle": {
      "enum": [
        "DRAFT",
        "REVIEW",
        "APPROVED",
        "FROZEN"
      ]
    },
    "trace_links": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "kind",
          "target_id"
        ],
        "properties": {
          "kind": {
            "enum": [
              "requirement",
              "risk",
              "adr",
              "test-intent"
            ]
          },
          "target_id": {
            "type": "string",
            "minLength": 1,
            "maxLength": 256
          }
        }
      }
    },
    "milestone_id": {
      "type": "string",
      "pattern": "^mil-[a-z0-9-]{2,59}$"
    }
  }
}
