import fs from 'node:fs'; import path from 'node:path'; import {fileURLToPath} from 'node:url';
const repo=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../../..'); const matrix=JSON.parse(fs.readFileSync(path.join(repo,'.devpilot/interfaces/uoc011_browser_state_matrix.json'),'utf8')); const routes=JSON.parse(fs.readFileSync(path.join(repo,'.devpilot/interfaces/ui_route_contract_registry.json'),'utf8'));
const required=new Set(matrix.required_states); const failures=[]; if(matrix.routes.length!==9)failures.push('routes-total'); if(required.size!==12)failures.push('states-total'); for(const r of routes.routes){if(!matrix.routes.some(m=>m.route_id===r.route_id))failures.push(`matrix:${r.route_id}`); for(const s of required){if(r.state_contract?.[s]!==true)failures.push(`${r.route_id}:${s}`);}}
console.log(JSON.stringify({schema_id:'devpilot.uoc011.state_matrix_smoke.v1',status:failures.length?'BLOCK':'PASS',routes_total:matrix.routes.length,states_total:required.size,cases_total:matrix.routes.length*required.size,failures},null,2)); process.exit(failures.length?2:0);
